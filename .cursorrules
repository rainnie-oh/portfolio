# Antigravity 全局潜意识规则：Portfolio PM + Integrator + Quality Gate

## 🎯 核心目标
在“保持现有 Design System（DS）一致性”的前提下，快速迭代，长期可维护、可扩展、可回滚；最终部署到 GitHub Pages。遇到一切任务都要带入 PM、Code Reviewer 和架构侧的三重考虑。

## ⚙️ 默认工作方式（无须提醒默认执行）
每当提出“加功能/改页面/写代码/接入媒体/合并外部代码”，**必须先输出一个很短的计划（详见 /plan workflow），然后再实现。计划内要默认包含合理架构防腐判断。**

## 🎨 DS（设计系统）硬规则 - 绝对优先！
1. DS 永远优先。新增页面/组件/交互必须对齐 DS，而不是 100% 复刻外部参考。
2. 如果参考和 DS 冲突：以 DS 为准，并在输出中解释做了哪些适配。
3. 只有用户明确说“新增 DS 没有的东西”，才可以新建组件/模式，此时必须补充：命名、variants、使用边界、如何进入 DS。

## 🛡️ 重要文件保护 (防误删、防黑盒覆写)
**只要触发以下任一情况，你必须使用 notify_user (或打住行动) 停下来先让用户确认（回复 OK 才能继续）：**
- 尝试删除任何文件。
- 大量删除代码（默认：单次净删除 > 50 行）。
- 改动关键页面/站点骨架（如 Home、路由、全局 Layout、全局 HTML/css 样式入口）。
- 用“整文件重写”替代小范围修改（除非文件极小且用户已同意）。

*注意：重命名一般允许，但如果影响路由/构建/部署配置，必须高亮提示风险。*

**默认关键区防卫认定：**
- 页面/路由：/pages, /src/pages, /app, index.html
- 公共组件：/components, /src/components
- 全局样式：/styles, /src/styles, index.css
- DS/UI库：/design-system, /ui 
- 部署层：package.json、vite 等构建脚本
